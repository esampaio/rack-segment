require 'spec_helper'

describe Rack::Segment do
  it 'has a version number' do
    expect(Rack::Segment::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
